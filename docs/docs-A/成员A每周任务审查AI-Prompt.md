# 成员 A 每周任务审查 AI Prompt

> 审查对象：成员 A  
> 技术方向：总体架构、API 契约、Gateway、用户认证与权限、最终联调  
> 使用方式：每周结束时复制 Prompt 正文，并补充成员 A 的提交、自述、接口文档变更、网关联调结果和测试报告。

## Prompt 正文

```text
我正在参与一个北京邮电大学 Web 后端开发课程项目：AI 驱动的“高并发秒杀与智能电商”后台系统。

请严格基于当前仓库中的 README.md 和 docs/ 规范文档，只审查“成员 A”的本周任务完成情况。不要审查成员 B 或成员 C，除非是说明 A 对他们造成的阻塞或依赖。

你必须优先阅读：

1. README.md
2. docs/总体架构设计.md
3. docs/API接口契约.md
4. docs/统一响应格式与错误码.md
5. docs/认证与权限规范.md
6. docs/数据库设计.md
7. docs/Redis-Key设计.md
8. docs/MQ消息契约.md
9. docs/部署与启动指南.md
10. docs/生产环境演进方案.md
11. docs/Apifox接口清单.md

成员 A 的固定技术方向：

- 总体架构
- 服务拆分
- API 契约
- Gateway
- 用户认证与权限
- Docker Compose 或统一启动方案
- Swagger/OpenAPI 汇总规范
- 最终联调和报告整合

当前项目关键口径：

- 所有客户端请求必须经过 Gateway。
- API 路径、字段、响应结构、错误码和权限以 docs/API接口契约.md 为准。
- JWT、角色和权限规则以 docs/认证与权限规范.md 为准。
- 统一响应格式以 docs/统一响应格式与错误码.md 为准。
- 跨服务变更必须先更新文档，再改代码。
- 成员 A 对接口契约和最终联调拥有最终裁定权。

本次审查周次：第【填写 1/2】周。

我将提供以下材料：

1. 成员 A 本周 git 提交摘要：
【粘贴】

2. 成员 A 本周自述：
【粘贴】

3. 文档更新清单：
【粘贴文件路径或内容摘要】

4. Gateway / user-service / 认证权限实现情况：
【粘贴】

5. 与成员 B 的接口、数据库、Redis、MQ 契约对齐情况：
【粘贴】

6. 与成员 C 的推送、测试页、Apifox 对齐情况：
【粘贴】

7. 本周自测报告和任务报告路径：
【粘贴，必须位于 docs/docs-A/ 下】

请输出成员 A 的独立审查报告，结构如下：

一、总体结论

- 成员 A 本周是否达到 README.md 和 docs/分工指南.md 中对应周次要求。
- 风险等级：低 / 中 / 高。
- 是否允许成员 A 分支合并到 dev。
- 如果不允许，列出阻塞项。

二、任务完成度审查

请按周次检查：

第 1 周重点：
- 是否完成总体架构、接口契约、统一响应、认证权限、部署启动等核心文档。
- 是否搭建 Gateway、user-service、common 基础骨架。
- Gateway 路由是否覆盖 auth/users/products/seckill/orders/ai/push。
- 用户注册、登录、管理员登录、当前用户、注销接口是否与文档一致。
- 是否定义并落地统一响应结构和错误码。
- 是否为 B/C 提供明确的接口、权限和启动约束。
- 是否提交 docs/docs-A/ 下的本周任务报告和测试报告。

第 2 周重点：
- Gateway 是否能完成统一入口、路由转发、CORS、基础日志和鉴权骨架。
- JWT 载荷、角色、权限判定是否与 docs/认证与权限规范.md 一致。
- 是否完成全链路联调：登录 -> 创建商品 -> 创建活动 -> 秒杀 -> MQ/订单 -> 推送 -> AI 咨询。
- 是否检查 B/C 的接口路径、响应格式、Redis key、MQ 字段是否破坏契约。
- 是否整理最终报告中架构、服务拆分、部署、生产演进和联调章节。
- 是否组织 Apifox 演示流程和 3-5 分钟视频主线。
- 是否提交 docs/docs-A/ 下的阶段任务报告和可复现测试报告。

三、契约一致性审查

请输出表格：

| 契约项 | 文档要求 | 实际情况 | 是否通过 | 修复建议 |

至少检查：

- Gateway 路由。
- 认证与权限。
- 统一响应格式。
- 错误码。
- API 路径。
- 环境变量。
- 启动顺序。
- 文档变更记录。

四、与 B/C 的依赖影响

请说明：

- A 是否阻塞 B 的秒杀、订单、MQ、数据库实现。
- A 是否阻塞 C 的推送、测试页、Apifox 整理。
- B/C 是否反过来阻塞 A 的联调。
- 需要谁在什么时候补齐什么。

五、问题列表

请按严重级别列出问题：

| 级别 | 问题 | 证据 | 负责人 | 修复建议 |

级别只能使用：

- P0：阻塞本周验收或合并。
- P1：影响演示质量或下周开发。
- P2：优化项。

六、下周任务建议

请只给成员 A 输出 5-8 条下周 TODO。每条必须具体到文件、接口、配置、联调动作或报告章节，不要泛泛建议。

七、合并建议

请明确输出：

- 是否允许 `feature/member-a-architecture` 合并到 `dev`。
- 合并前必须补齐什么。
- 合并后需要通知 B/C 什么。
```

