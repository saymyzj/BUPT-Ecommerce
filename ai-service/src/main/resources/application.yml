server:
  port: 8084

spring:
  application:
    name: ai-service

app:
  product-service:
    base-url: http://localhost:8082
  llm:
    base-url: ${LLM_BASE_URL:}
    api-key: ${LLM_API_KEY:}
    model: ${LLM_MODEL:gpt-4o-mini}
    timeout-ms: 8000
