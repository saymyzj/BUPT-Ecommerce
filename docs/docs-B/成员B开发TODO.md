# 成员 B 开发状态

本文按当前代码和阶段报告重新整理成员 B 的任务状态，避免旧 TODO 勾选项被误读为未完成工作。开发仍以 `README.md`、`docs/API接口契约.md`、`docs/数据库设计.md`、`docs/Redis-Key设计.md`、`docs/MQ消息契约.md`、`docs/认证与权限规范.md` 为准。

## 已完成

1. 商品、库存、秒杀活动实体与 Repository 已落地，接口覆盖商品创建、分页、详情、库存设置、活动创建与活动详情。
2. 秒杀链路已接入 Redis key、Lua 原子扣减、用户参与标记和秒杀结果缓存。
3. 秒杀成功后返回 `queued` / `QUEUEING`，不在秒杀接口同步等待订单落库。
4. RabbitMQ 下单消息、交换机、队列、死信队列和路由键已配置。
5. `order-service` 已实现 MQ 消费、订单主表和明细表落库、消息幂等日志、失败重试与补偿任务。
6. 订单查询接口已覆盖用户订单列表、用户订单详情、管理员订单分页查询。
7. 秒杀结果查询可从 Redis 读取，并通过 `order-service` 内部回查接口补充订单创建结果。
8. Apifox 截图、阶段测试报告和自审总结已记录核心链路、重复抢购、防超卖和订单落库验证。

## 待完成

1. 继续和成员 A 联调 Gateway 注入的 `X-User-Id`、`X-User-Role` 等用户上下文，确保所有客户端请求只走 Gateway。
2. 演示环境需要默认用户兜底时，必须显式启用 `demo` profile；默认配置下缺少 Gateway 用户头应返回未登录。
3. 和成员 C 继续确认测试页面、Apifox 集合、演示脚本中的订单和秒杀结果字段是否与最新 API 契约一致。
4. 如需保留服务间接口 `GET /api/orders/internal/seckill-result`，需持续在 API 契约和 Apifox 清单中标注为内部接口。
5. 生产演进项仍需补充更完整的限流、死信告警、消息回放、人工补偿和监控说明。
6. `ai_consult_logs` 当前不实现落库；若后续报告要强调 AI 咨询日志能力，需要补 schema、实体、Repository 和写入逻辑。

## 风险项

1. 本地 MySQL、Redis、RabbitMQ 未启动或演示数据未预热时，秒杀到订单链路会失败，需要按部署文档提前准备环境。
2. 默认用户兜底若在非演示环境开启，会与“服务端只信任 Gateway 注入上下文头”的认证规范冲突。
3. 多服务各自执行 `schema.sql`，需要保证建表脚本幂等，避免重复初始化影响联调。
4. `order-service` 内部回查接口已由 Gateway 拦截并要求内部 token；生产环境仍建议叠加内网访问控制。
5. 高并发压测结论依赖 Redis、RabbitMQ 和 MySQL 的运行状态，验收前需要重新执行一轮可复现测试。
