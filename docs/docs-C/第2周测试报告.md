# 成员 C 第 2 周测试报告

## 1. 测试目标

验证成员 C 第 2 周完整链路是否满足项目验收要求：

1. 秒杀接口只返回排队状态，不伪造订单成功。
2. 订单成功必须来自 MQ 落库后的推送或订单查询。
3. SSE 推送与用户隔离正确。
4. AI 咨询基于真实商品上下文，支持真实 LLM 成功和明确降级。
5. Apifox 可按完整流程组织和复现。
6. 演示素材可支撑登录、商品、秒杀、订单、AI 和推送闭环。

## 2. 测试环境

| 项目 | 值 |
| --- | --- |
| Gateway | `http://localhost:8080` |
| MySQL | 本地 3306 |
| Redis | 本地 6379 |
| RabbitMQ | 本地 5672 / 15672 |
| 测试页 | `simple-test-page/index.html` |
| Apifox 环境 | `BUPT Ecommerce Local` |
| 分支 | `feature/member-c-ai-push-demo` |

## 3. 完整链路测试

| 编号 | 测试项 | 操作 | 预期结果 | 实际结果 | 证据 |
| --- | --- | --- | --- | --- | --- |
| C-W2-01 | 服务启动 | 启动 MySQL、Redis、RabbitMQ 和后端服务 | 各服务可访问 | 通过 | `00-service-status.png` |
| C-W2-02 | 管理员登录 | 调用管理员登录 | 返回 `ADMIN` token | 通过 | `01-admin-login.png` |
| C-W2-03 | 管理员准备商品 | 创建商品、设置库存、创建活动 | 生成 `productId`、`activityId` | 通过 | `02-admin-product-activity.png` |
| C-W2-04 | 用户登录和推送连接 | 注册/登录普通用户并建立 SSE | 角色为 `CUSTOMER`，推送连接成功 | 通过 | `03-user-login-sse-connected.png` |
| C-W2-05 | 发起秒杀 | 调用秒杀下单接口 | 返回 `QUEUEING`，不直接显示订单成功 | 通过 | `04-seckill-queueing.png` |
| C-W2-06 | 订单推送 | 等待 MQ 消费和订单落库 | SSE 收到 `ORDER_CREATED` | 通过 | `05-order-created-push.png` |
| C-W2-07 | 订单详情 | 使用推送得到的 `orderId` 查询订单详情 | 返回订单详情和状态 | 通过 | `06-order-detail-ai-consult.png` |
| C-W2-08 | AI 咨询 | 调用 AI 商品咨询 | 返回 `SUCCESS` 或明确 `FALLBACK` | 通过 | `06-order-detail-ai-consult.png` |
| C-W2-09 | Apifox 分组 | 查看 Apifox 集合 | 8 个分组完整 | 通过 | `07-apifox-groups.png` |
| C-W2-10 | Apifox 环境变量 | 查看本地环境变量 | `baseUrl` 和链路变量完整 | 通过 | `08-apifox-env.png` |
| C-W2-11 | Apifox 完整流程 | 执行 `08 完整演示流程` | 12 步请求可连续验证 | 通过 | `09-apifox-12-steps.png` |

## 4. 推送专项测试

| 编号 | 测试项 | 方法 | 结果 |
| --- | --- | --- | --- |
| PUSH-01 | CONNECTED 不冒充订单成功 | 建立 SSE 后观察页面状态 | 只显示推送连接成功，不显示订单创建 |
| PUSH-02 | 订单落库后推送 | 发起秒杀并等待 MQ 消费 | 收到 `ORDER_CREATED` |
| PUSH-03 | 用户隔离 | A/B 两个用户分别建立 SSE，A 发起秒杀 | A 收到订单事件，B 未收到 A 的订单事件 |
| PUSH-04 | 断线重连 | 断开 A 的 SSE 后重新连接，再发起秒杀 | 重连后可继续收到新订单事件 |
| PUSH-05 | 推送失败兜底 | 说明推送失败时的验证方式 | 仍可通过订单列表或订单详情查询验证结果 |

## 5. AI 专项测试

| 编号 | 测试项 | 预期 | 结果 |
| --- | --- | --- | --- |
| AI-01 | 未配置 LLM | 返回 `30001` 和 `FALLBACK`，不伪装成功 | 通过 |
| AI-02 | 配置 LLM | 调用 OpenAI 兼容 `chat/completions` | 通过 |
| AI-03 | 商品上下文 | 请求包含商品名称、价格、库存等上下文 | 通过 |
| AI-04 | Redis 缓存 | 相同问题二次咨询命中缓存 | 通过 |
| AI-05 | 生产演进说明 | 说明缓存、限流、排队、超时降级 | 通过 |

## 6. Apifox 测试

正式导出文件：

```text
apifox/BUPT-Ecommerce-Member-C.postman_collection.json
apifox/BUPT-Ecommerce-local.postman_environment.json
```

Apifox 侧已完成：

1. 8 个分组按 `docs/Apifox接口清单.md` 组织。
2. `baseUrl` 指向 `http://localhost:8080`。
3. 登录后提取 `token`、`adminToken`。
4. 创建商品后提取 `productId`。
5. 创建活动后提取 `activityId`。
6. 查询订单后提取 `orderId`。
7. `08 完整演示流程` 可按顺序执行。

## 7. 视频与截图验收

```text
docs/docs-C/videos/member-c-demo.webm
docs/docs-C/videos/member-c-demo.srt
```

视频时长约 4 分 05 秒，覆盖：

1. Gateway 与权限。
2. 管理员准备商品、库存、活动。
3. 用户注册登录和 SSE 连接。
4. 秒杀 `QUEUEING`。
5. `ORDER_CREATED` 推送。
6. 订单详情。
7. AI 咨询。
8. Apifox 分组、环境变量、12 步流程。
9. 生产演进。

## 8. 测试结论

第 2 周测试通过。成员 C 当前已具备可复现的完整演示链路，且没有用静态假数据冒充订单成功或 AI 成功。合并前还需要完成 Git 提交、推送，并由 A/B 对 Gateway 权限、订单事件字段和最终演示顺序做联合确认。

